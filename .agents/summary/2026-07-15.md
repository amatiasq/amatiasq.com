# Exploración 2026-07-15 — lo que sigue vivo

Revisado el 2026-08-07: de toda la exploración sólo sobrevive un hallazgo,
porque el resto lo dice el código.

**Hay dos sistemas de i18n y el segundo está roto.** `internals/i18n.ts` guarda
el idioma en un `let` de módulo que nadie inicializa —`setLanguage()` no se
llama en ninguna parte—, así que siempre vale `'en'`. Lo usa sólo `Image.astro`,
de modo que un `src` o `alt` traducible sale en inglés también en `/es/`. Se
arregla usando `useTranslations(Astro.url)` como todo lo demás; entonces
`internals/i18n.ts` se queda sin usuarios y se borra entero.

Qué más quedó propuesto: [`update-2026-07-15.md`](update-2026-07-15.md).
