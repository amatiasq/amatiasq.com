---
import { readCollectionAsPages } from '@internals/content';
import Showcase from '@components/templates/Showcase.astro';

export function getStaticPaths() {
  return readCollectionAsPages('projects');
}

const { entry } = Astro.props;
---

<Showcase entry={entry} />
